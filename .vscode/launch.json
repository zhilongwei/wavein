{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug WaveIn",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/dev/src/apps/wavein",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build dev"
    }
  ]
}
